---
import SpeedInsights from "@vercel/speed-insights/astro";
import Analytics from "@vercel/analytics/astro";

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="author" content="Dariels Martinez" />
    <meta
      name="description"
      content="Entusiasta de 22 años de Honduras 🇭🇳, especializado en Desarrollo de Aplicaciones Web. Experiencia en proyectos digitales y comprometido con la innovación. Busco oportunidades para crecer y colaborar en el ámbito tecnológico."
    />
    <meta
      name="keywords"
      content="Dariels Martinez, desarrollador web, portafolio digital, proyectos web, diseño y programación"
    />
    <meta name="robots" content="index, follow" />
    <meta content="width=device-width, initial-scale=1" name="viewport" />
    <meta property="og:url" content="https://darielsmtz.vercel.app/" />
    <meta property="og:type" content="website" />
    <meta
      property="og:image"
      content="https://darielsmtz.vercel.app/darielsmtz.vercel.app_.png"
    />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:locale" content="es_ES" />
    <meta property="og:title" content="Dariels Martinez ✦ Desarrollador Web" />
    <meta
      property="og:image:alt"
      content="Dariels Martinez ✦ Desarrollador Web"
    />
    <meta
      property="og:site_name"
      content="Dariels Martinez ✦ Desarrollador Web"
    />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="apple-touch-icon" href="/favicon.svg" />
    <meta http-equiv="x-ua-compatible" content="IE=edge; chrome=1" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <script src="https://app.embed.im/snow.js" defer></script>
    <meta
      name="google-site-verification"
      content="MH-RTlA7Qp1S1hpOo44gPj2VKbB8OsTVA_5TXsljbQE"
    />
  </head>
  <body class="⁓space-y-2/16">
    <SpeedInsights />
    <Analytics />
    <slot />
  </body>
</html>

<style is:global>
  :root {
    --accent: 136, 58, 234;
    --accent-light: 224, 204, 250;
    --accent-dark: 49, 10, 101;
    --accent-gradient: linear-gradient(
      45deg,
      rgb(var(--accent)),
      rgb(var(--accent-light)) 30%,
      white 60%
    );
  }

  html {
    font-family: Arial, Helvetica, sans-serif;
    background: #f6f5f1;
    color: #13100f;
  }
  code {
    font-family:
      Menlo,
      Monaco,
      Lucida Console,
      Liberation Mono,
      DejaVu Sans Mono,
      Bitstream Vera Sans Mono,
      Courier New,
      monospace;
  }
  body {
    position: relative;
    min-height: 100vh;
    z-index: 1;
    text-rendering: optimizeLegibility;
    -webkit-font-smoothing: antialiased;
  }
  body::before {
    content: "";
    position: absolute;
    inset: 0px;
    z-index: -100;
    background-image: url(/svg/header-gradient.svg),
      url(/svg/footer-gradient.svg);
    background-repeat: no-repeat;
    background-position:
      center top,
      center bottom;
    background-size: 1400px;
  }

  #texture {
    position: fixed;
    top: 0px;
    left: 0px;
    z-index: -99;
    width: 100%;
    height: calc(200px + 100vh);
    opacity: 0.25;
    pointer-events: none;
    transform: translateY(0px);
    filter: contrast(120%) brightness(120%);
  }

  @font-face {
    font-family: "Soria";
    src:
      url("/font/soria-font-webfont.woff2") format("woff2"),
      url("/font/soria-font-webfont.woff") format("woff");
    font-weight: normal;
    font-style: normal;
    font-display: swap;
  }

  /* Define la animación */
  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  .fade-in {
    opacity: 0;
    animation: fadeIn 1s ease-in forwards;
  }

  .animate-on-scroll {
    opacity: 0;
    transform: translateY(80px);
    transition:
      opacity 1.5s ease-in-out,
      transform 1.5s ease-in-out;
  }

  .animate-on-scroll.visible {
    opacity: 1;
    transform: translateY(0);
  }
</style>
